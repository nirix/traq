<div class="content">
	<?php echo HTML::link(l('new_role'), '/admin/roles/new', array('class' => 'button_new', 'data-overlay' => true)); ?>
</div>
<div>
	<table class="list">
		<thead>
			<th class="name"><?php echo l('name'); ?></th>
			<th><?php echo l('assignable'); ?></th>
			<th class="project"><?php echo l('project'); ?></th>
			<th class="actions"><?php echo l('actions'); ?></th>
		</thead>
		<tbody>
		<?php foreach ($roles as $role) { ?>
			<tr>
				<td><?php echo HTML::link($role->name, "/admin/roles/{$role->id}/edit", array('data-overlay' => true)); ?></td>
				<td><?php echo $role->assignable ? '&#10003;' : '&times;' ?></td>
				<td><?php echo $role->project_id > 0 ? $role->project->name : l('all'); ?></td>
				<td>
					<?php echo HTML::link(l('edit'), "/admin/roles/{$role->id}/edit", array('class' => 'button_edit', 'data-overlay' => true)); ?>
					<?php echo HTML::link(l('delete'), "/admin/roles/{$role->id}/delete", array('class' => 'button_delete', 'data-confirm' => l('confirm.delete_x', $role->name))); ?>
				</td>
			</tr>
		<?php } ?>
		</tbody>
	</table>
</div>
