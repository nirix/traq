<div class="usercp container">
	<h1 class="page-header"><?=$t('usercp')?></h1>
	<?=View::render('UserCP/_nav')?>
	<div class="usercp-information">
		<?=Errors::show($user->errorMessages())?>

		<form action="<?=Request::basePath('/usercp')?>" class="form-horizontal" role="form">
			<div class="container-fluid">
				<div class="row">
					<fieldset id="info" class="col-md-6">
						<legend><?=$t('information')?></legend>

						<div class="form-group">
							<?=Form::label($t('name'), 'name', ['class' => 'control-label col-sm-2'])?>
							<div class="col-sm-10">
								<?=Form::text('name', ['value' => $user->name, 'class' => 'form-control'])?>
							</div>
						</div>

						<div class="form-group">
							<?=Form::label($t('email'), 'email', ['class' => 'control-label col-sm-2'])?>
							<div class="col-sm-10">
								<?=Form::text('email', ['value' => $user->email, 'class' => 'form-control'])?>
							</div>
						</div>

						<div class="form-group">
							<?=Form::label($t('api_key'), 'api_key', ['class' => 'col-sm-2 control-label'])?>
							<div class="col-sm-10">
								<?php if ($user->api_key != null) { ?>
									<?=Form::text('api_key', [
										'value'    => $user->api_key,
										'readonly' => true,
										'class'    => 'form-control'
									])?>
								<?php } ?>
								<?=HTML::link($t($user->api_key ? 'renew' : 'create'), 'usercp/create_api_key', [
									'class' => 'btn btn-sm'
								])?>
							</div>
						</div>
					</fieldset>

					<fieldset id="info" class="col-md-6">
						<legend><?=$t('options')?></legend>
					</fieldset>
				</div>
			</div>
		</form>

	</div>
</div>
