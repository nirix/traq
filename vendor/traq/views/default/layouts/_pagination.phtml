<?php if ($pagination->paginate) : ?>
	<div class="content">
		<div class="pagination">
			<?php if ($pagination->prev_page_url) : ?>
				<a href="<?= $pagination->prev_page_url; ?>" class="previous">
					<span class="fas fa-fw fa-angle-left"></span>
					<span><?= l('previous'); ?></span>
				</a>
			<?php else : ?>
				<div class="previous">
					<span class="fas fa-fw fa-angle-left"></span>
					<span><?= l('previous'); ?></span>
				</div>
			<?php endif; ?>

			<div class="pages"><?= l('x_of_x', ['page' => $pagination->page, 'total' => $pagination->total_pages]) ?></div>

			<?php if ($pagination->next_page_url) : ?>
				<a href="<?= $pagination->next_page_url; ?>" class="next">
					<span><?= l('next'); ?></span>
					<span class="fas fa-fw fa-angle-right"></span>
				</a>
			<?php else : ?>
				<div class="next">
					<span><?= l('next'); ?></span>
					<span class="fas fa-fw fa-angle-right"></span>
				</div>
			<?php endif; ?>
		</div>
	</div>
<?php endif; ?>
