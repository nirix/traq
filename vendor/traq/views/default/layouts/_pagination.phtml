<?php if ($pagination->paginate) : ?>
	<div class="content">
		<div class="pagination">
			<?php if ($pagination->prev_page_url) : ?>
				<a href="<?= $pagination->prev_page_url; ?>" class="btn-secondary">
					<span class="fas fa-fw fa-angle-left"></span>
					<span><?= l('previous'); ?></span>
				</a>
			<?php endif; ?>
			<?php if ($pagination->next_page_url) : ?>
				<a href="<?= $pagination->next_page_url; ?>" class="btn-secondary">
					<span><?= l('next'); ?></span>
					<span class="fas fa-fw fa-angle-right"></span>
				</a>
			<?php endif; ?>
		</div>
	</div>
<?php endif; ?>
