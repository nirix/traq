<div class="group">
	<label><?php echo l('slug'); ?></label>
	<?php echo Form::text('slug', array('value' => $repo->slug)); ?> <abbr title="<?php echo l('help.slug'); ?>">?</abbr>
</div>
<div class="group">
	<label><?php echo l('type'); ?></label>
	<?php echo Form::select('type', scm_select_options(), array('value' => $repo->type)); ?>
</div>
<?php
foreach (scm_types() as $scm => $name) {
	View::render("project_settings/repositories/adapters/_{$scm}");
}
?>