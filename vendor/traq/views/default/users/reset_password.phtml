<section id="forgot_password_form" class="content">
	<div>
		<h2 id="page_title"><?= l('forgot_password'); ?></h2>
		<?php if (isset($password_reset) and $password_reset = true) { ?>
			<div class="panel">
				<?= l('password_reset.success', $new_password); ?>
			</div>
		<?php } elseif (isset($reset_email_sent) and $reset_email_sent == true) { ?>
			<div class="panel">
				<?= l('password_reset.email_sent'); ?>
			</div>
		<?php } else { ?>
			<?php if (isset($error) and $error) { ?>
				<div class="alert-danger">
					<?= l('errors.invalid_username'); ?>
				</div>
			<?php } ?>
			<form action="<?= Request::base('login/resetpassword'); ?>" method="post">
				<div class="panel">
					<div class="panel-content">
						<div class="form-group-row">
							<label><?= l('username'); ?></label>
							<?= Form::text('username'); ?>
						</div>
					</div>

					<div class="panel-footer">
						<input type="submit" value="<?= l('reset'); ?>" class="btn-primary" />
					</div>
				</div>
			</form>
		<?php } ?>
	</div>
</section>
