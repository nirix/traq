<div class="admin custom_tabs listing content">
	<?= HTML::link(l('new_tab'), '/admin/custom_tabs/new', ['class' => 'btn-success', 'data-overlay' => true, 'icon' => 'fas fa-fw fa-plus']); ?>
</div>
<div>
	<table class="list">
		<thead>
			<th><?= l('name'); ?></th>
			<th class="w-48 text-right"><?= l('actions'); ?></th>
		</thead>
		<tbody>
			<?php foreach ($custom_tabs as $tab) : ?>
				<tr>
					<td><?= HTML::link($tab->label, "/admin/custom_tabs/{$tab->id}/edit", array('data-overlay' => true)); ?></td>
					<td>
						<div class="btn-group justify-end">
							<?= HTML::link(l('edit'), "/admin/custom_tabs/{$tab->id}/edit", ['class' => 'btn-warning btn-sm', 'data-overlay' => true, 'icon' => 'fas fa-fw fa-pencil', 'sr-only' => true]); ?>
							<?= HTML::link(l('delete'), "/admin/custom_tabs/{$tab->id}/delete", ['class' => 'btn-danger btn-sm', 'data-confirm' => l('confirm.delete_x', $tab->label), 'icon' => 'fas fa-fw fa-trash', 'sr-only' => true]); ?>
						</div>
					</td>
				</tr>
			<?php endforeach; ?>
		</tbody>
	</table>
</div>
