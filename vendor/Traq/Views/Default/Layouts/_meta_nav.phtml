<nav id="nav-meta" class="navbar navbar-inverse navbar-static-top" role="navigation">
	<div class="container">
		<ul class="links-site nav navbar-nav navbar-left">
		<?php if (settings('site_name')): ?>
			<li><?php echo HTML::link(settings('site_name'), settings('site_url')); ?></li>
		<?php endif; ?>
		<?php if (isset($project)): ?>
			<li><?php echo HTML::link(settings('title'), '/'); ?></li>
		<?php endif; ?>
		</ul>
		<ul class="links-user nav navbar-nav navbar-right">
		<?php if (LOGGEDIN): ?>
			<?php if ($currentUser->group()->is_admin or (isset($project) and $currentUser->permission($project->id, 'project_settings'))): ?>
			<li class="dropdown">
				<?php echo HTML::link(
					TWBS::fa('cog') . " " . TWBS::caret(), '#',
					array('class' => "dropdown-toggle", 'data-toggle' => "dropdown")
				); ?>
				<ul class="dropdown-menu">
				<?php if (isset($project)): ?>
					<li><?php echo HTML::link($t('project_settings'), $project->href('settings')); ?></li>
				<?php endif; ?>
				<?php if ($currentUser->group()->is_admin): ?>
					<li><?php echo HTML::link($t('admincp'), '/admin'); ?></li>
				<?php endif; ?>
				</ul>
			</li>
			<?php endif; ?>
			<li class="dropdown">
				<?php echo HTML::link(
					Gravatar::forUser($currentUser, 18) . " " . TWBS::caret(), '/usercp',
					array('class' => "dropdown-toggle", 'data-toggle' => "dropdown")
				); ?>
				<ul class="dropdown-menu">
					<li><?php echo HTML::link($t('profile'), $currentUser->href()); ?></li>
					<li><?php echo HTML::link($t('usercp'), '/usercp'); ?></li>
					<li><?php echo HTML::link($t('logout'), '/logout'); ?></li>
				</ul>
			</li>
		<?php else: ?>
			<li class="dropdown">
				<?php echo HTML::link(
					TWBS::fa('user') . " " . TWBS::caret(), '/usercp',
					array('class' => "dropdown-toggle", 'data-toggle' => "dropdown")
				); ?>
				<ul class="dropdown-menu">
					<li><?php echo HTML::link($t('login'), '/login'); ?></li>
					<li><?php echo HTML::link($t('register'), '/register'); ?></li>
				</ul>
			</li>
		<?php endif; ?>
		</ul>
	</div>
</nav>
