<div class="admin-projects container">
	<h1 class="page-header"><?php echo $t('projects'); ?></h1>

	<div class="page-actions">
		<?php echo HTML::link(
			TWBS::fa('plus', $t('new_project')), '/admin/projects/new',
			array(
				'data-overlay' => true,
				'class' => 'btn btn-success'
			)
		); ?>
	</div>

	<table class="table table-striped table-hover">
		<thead>
			<th class="name"><?php echo $t('name'); ?></th>
			<th class="codename"><?php echo $t('codename'); ?></th>
			<th class="actions"></th>
		</thead>
		<tbody>
		<?php foreach ($projects as $project): ?>
			<tr>
				<td>
					<?php echo HTML::link(
						$project->name, "/admin/projects/{$project->id}/edit",
						array('data-overlay' => true)
					); ?>
				</td>
				<td>
					<?php if ($project->codename): ?>
						<span class="project-codename"><?php echo $project->codename; ?></span>
					<?php endif; ?>
				</td>
				<td class="actions">
					<?php echo HTML::link(
						TWBS::fa('edit', $t('edit')), "/admin/projects/{$project->id}/edit",
						array('data-overlay' => true, 'class' => 'btn btn-xs btn-warning')
					); ?>
					<?php echo HTML::link(
						TWBS::fa('times', $t('delete')), "/admin/projects/{$project->id}/delete",
						array('data-confirm' => $t('confirm.delete'), 'class' => 'btn btn-xs btn-danger')
					); ?>
				</td>
			</tr>
		<?php endforeach; ?>
		</tbody>
	</table>
</div>
