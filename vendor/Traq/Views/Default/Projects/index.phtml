<div class="project-listing container">
	<h2 id="page_title"><?php echo $t('projects'); ?></h2>
	<ul id="project_list">
		<?php foreach($projects as $project) { ?>
		<li>
			<h3><?php echo HTML::link($project->name, $project->slug); ?><?php echo ($project->codename ? " <em>\"{$project->codename}\"</em>" : ''); ?></h3>
			<nav>
				<ul>
					<li><?php echo HTML::link($t('roadmap'), "{$project->slug}/roadmap"); ?></li>
					<li><?php echo HTML::link($t('tickets'), "{$project->slug}/tickets"); ?></li>
					<li><?php echo HTML::link($t('timeline'), "{$project->slug}/timeline"); ?></li>
					<?php Hook::run('template:projects/index/project/nav', array($project)); ?>
				</ul>
			</nav>
			<div class="description">
				<?php echo Format::text($project->info); ?>
			</div>
			<?php Hook::run('template:projects/index/project', array($project)); ?>
		</li>
		<?php } ?>
	</ul>
</div>
