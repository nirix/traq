<div class="roadmap container">
	<h1 class="page-header"><?php echo $t('roadmap'); ?></h1>

	<nav>
		<ul class="nav nav-tabs">
			<li<?php echo activeNav('/:slug/roadmap') ? ' class="active"' : ''; ?>>
				<?php echo HTML::link($t('active'), $project->href('roadmap')); ?>
			</li>
			<li<?php echo activeNav('/:slug/roadmap/completed') ? ' class="active"' : ''; ?>>
				<?php echo HTML::link($t('completed'), $project->href('roadmap/completed')); ?>
			</li>
			<li<?php echo activeNav('/:slug/roadmap/all') ? ' class="active"' : ''; ?>>
				<?php echo HTML::link($t('all'), $project->href('roadmap/all')); ?>
			</li>
		</ul>
	</nav>

	<ul class="milestones">
<?php foreach ($milestones as $milestone): ?>
		<li>
			<h2>
				<?php echo HTML::link($milestone->name, $milestone); ?>
				<?php if ($milestone->codename): ?>
					<small><em class="codename"><?php echo $milestone->codename; ?></em></small>
				<?php endif; ?>
			</h2>
			<?php echo View::render('Roadmap/_progressBar', array('milestone' => $milestone)); ?>
			<ul class="issue-counts">
				<li>
					<?php echo HTML::link(
						$t('x_open', array($milestone->openTicketCount())),
						$project->href('issues/?open')
					); ?>
				</li>
				<li>
					<?php echo HTML::link(
						$t('x_started', array($milestone->startedTicketCount())),
						$project->href('issues/?started')
					); ?>
				</li>
				<li>
					<?php echo HTML::link(
						$t('x_closed', array($milestone->closedTicketCount())),
						$project->href('issues/?closed')
					); ?>
				</li>
			</ul>
			<div>
				<?php echo Format::text($milestone->info); ?>
			</div>
		</li>
<?php endforeach; ?>
	</ul>
</div>
