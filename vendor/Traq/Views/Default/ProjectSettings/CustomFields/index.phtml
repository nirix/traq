<div class="content">
	<h2 id="page_title"><?php echo l('project_settings'); ?></h2>
</div>
<?php echo View::render('project_settings/_nav'); ?>
<div class="content">
	<?php echo HTML::link(l('new_field'), "{$project->slug}/settings/custom_fields/new", array('class' => 'button_new', 'data-overlay' => true)); ?>
</div>
<div>
	<table class="list">
		<thead>
			<tr>
				<th class="name"><?php echo l('name'); ?></th>
				<th class="type"><?php echo l('type'); ?></th>
				<th class="actions"><?php echo l('actions'); ?></th>
			</tr>
		</thead>
		<tbody>
		<?php foreach ($custom_fields as $field) { ?>
			<tr>
				<td><?php echo HTML::link($field->name, "{$project->slug}/settings/custom_fields/{$field->id}/edit", array('data-overlay' => true)); ?></td>
				<td><?php echo l($field->type); ?></td>
				<td>
					<?php echo HTML::link(l('edit'), "{$project->slug}/settings/custom_fields/{$field->id}/edit", array('class' => 'button_edit', 'data-overlay' => true)); ?>
					<?php echo HTML::link(l('delete'), "{$project->slug}/settings/custom_fields/{$field->id}/delete", array('class' => 'button_delete', 'data-confirm' => l('confirm.delete_x', $field->name))); ?>
				</td>
			</tr>
		<?php } ?>
		</tbody>
	</table>
</div>
