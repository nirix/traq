<?php extendView('layouts/default.phtml'); ?>

<div class="milestone content">
	<h2 class="page-title"><?= $milestone->name; ?></h2>
	<div class="mb-4">
		<?= subscription_link_for($milestone); ?>
	</div>
	<?= View::render('milestones/_progress', ['row' => $milestoneData]); ?>
	<div class="milestone_info">
		<?= format_text($milestone->info); ?>
	</div>
</div>
