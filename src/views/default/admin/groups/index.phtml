<?php extendView('layouts/admin.phtml'); ?>

<div class="content">
	<?= HTML::link(l('new_group'), '/admin/groups/new', array('class' => 'btn-success', 'data-overlay' => true, 'icon' => 'fas fa-fw fa-plus')) ?>
</div>
<div>
	<table class="list">
		<thead>
			<th class="w-52"><?= l('name') ?></th>
			<th><?= l('users') ?></th>
			<th class="w-48 text-right"><?= l('actions') ?></th>
		</thead>
		<tbody>
			<?php foreach ($groups as $group) : ?>
				<tr>
					<td><?= HTML::link($group->name, "/admin/groups/{$group->id}/edit", array('data-overlay' => true)) ?></td>
					<td><?= $group->users->exec()->row_count() ?></td>
					<td>
						<div class="btn-group justify-end">
							<?= HTML::link(l('edit'), "/admin/groups/{$group->id}/edit", ['class' => 'btn-warning btn-sm', 'data-overlay' => true, 'icon' => 'fas fa-fw fa-pencil', 'sr-only' => true]) ?>
							<a href="/admin/groups/<?= $group->id ?>/delete" class="btn-danger btn-sm"
								x-data="popoverConfirm({ position: 'center', message: '<?= l('confirm.delete') ?>', post: true })"
								@click.prevent="toggle()">
								<i class="fas fa-fw fa-trash"></i>
								<span class="sr-only"><?= l('delete') ?></span>
							</a>
						</div>
					</td>
				</tr>
			<?php endforeach; ?>
		</tbody>
	</table>
</div>
