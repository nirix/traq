<?php if (isset($update) && $update['code'] > TRAQ_VER_CODE) { ?>
	<div class="alert alert-warning">
		<?php echo l('traq_update_available', $update['name'], $update['version'], $update['download'], $update['announcement']); ?>
	</div>
<?php } ?>
