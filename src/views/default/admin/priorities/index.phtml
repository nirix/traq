<?php extendView('layouts/admin.phtml'); ?>

<div class="content">
	<?= HTML::link(l('new_priority'), '/admin/priorities/new', array('class' => 'btn-success', 'data-overlay' => true, 'icon' => 'fas fa-fw fa-plus')) ?>
</div>
<div>
	<table class="list">
		<thead>
			<th><?= l('name') ?></th>
			<th class="w-48 text-right"><?= l('actions') ?></th>
		</thead>
		<tbody>
			<?php foreach ($priorities as $priority) : ?>
				<tr>
					<td><?= HTML::link($priority->name, "/admin/priorities/{$priority->id}/edit", array('data-overlay' => true)) ?></td>
					<td>
						<div class="btn-group justify-end">
							<?= HTML::link(l('edit'), "/admin/priorities/{$priority->id}/edit", ['class' => 'btn-warning btn-sm', 'data-overlay' => true, 'icon' => 'fas fa-fw fa-pencil', 'sr-only' => true]) ?>
							<a href="/admin/priorities/<?= $priority->id ?>/delete" class="btn-danger btn-sm"
								x-data="popoverConfirm({ position: 'center', message: '<?= l('confirm.delete') ?>', post: true })"
								@click.prevent="toggle()">
								<i class="fas fa-fw fa-trash"></i>
								<span class="sr-only"><?= l('delete') ?></span>
							</a>
						</div>
					</td>
				</tr>
			<?php endforeach; ?>
		</tbody>
	</table>
</div>
