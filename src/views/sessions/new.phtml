<div class="new-session container">
	<div class="row">
		<div class="col-md-6 col-md-offset-3">
			<h1 class="page-header"><?=t('login')?></h1>

			<?php if (Request::$method == 'POST') : ?>
				<?php if (isset($activationRequired) && $activationRequired) : ?>
					<?=TWBS::alert(t('errors.account.activation_required'), 'warning', false)?>
				<?php elseif (isset($error) && $error) : ?>
					<?=TWBS::alert(t('errors.invalid_username_or_password'), 'danger', false)?>
				<?php endif; ?>
			<?php endif ?>

			<form action="<?=routeUrl('session_create')?>" method="post" class="form-horizontal">
				<?=View::render('sessions/_form.phtml')?>

				<div class="form-actions">
					<button type="submit" class="btn btn-success"><?=t('login')?></button>
				</div>
			</form>
		</div>
	</div>
</div>
