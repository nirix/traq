<div class="new-session container">
	<div class="row">
		<div class="col-md-6 col-md-offset-3">
			<h1><?=$t('login')?></h1>

			<?php if (Request::method() == 'POST'): ?>
				<?php if (isset($activationRequired)): ?>
					<?=TWBS::alert($t('errors.account.activation_required'), 'danger', false)?>
				<?php else: ?>
					<?=TWBS::alert($t('errors.invalid_username_or_password'), 'danger', false)?>
				<?php endif; ?>
			<?php endif; ?>
			<form action="<?=Request::basePath('login')?>" method="post" class="form-horizontal">
				<?=View::render('sessions/_form.phtml')?>
				<div class="form-actions">
					<?=Form::submit($t('login'), array('class' => 'btn btn-success'))?>
				</div>
			</form>
		</div>
	</div>
</div>
