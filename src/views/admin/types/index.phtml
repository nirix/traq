<?php $this->extend('layouts/admin.phtml'); ?>

<?php $this->startSection('content'); ?>
<div class="admin admin-types content container">
	<h1 class="page-header"><?=t('types')?></h1>

	<div class="page-actions">
		<a href="<?=routeUrl('admin_new_type')?>" class="btn btn-success" data-remote-modal="#new-type-modal">
			<span class="fa fa-plus"></span> <?=t('new_type')?>
		</a>
	</div>

	<table class="table table-striped table-hover">
		<thead>
			<tr>
				<th><?=t('name')?></th>
				<th></th>
			</tr>
		</thead>
		<tbody>
		<?php foreach ($types as $type) : ?>
			<tr>
				<td>
					<a href="<?=routeUrl('admin_edit_type', ['id' => $type['id']])?>" data-remote-modal="#edit-type-modal">
						<?=$type['name']?>
					</a>
				</td>
				<td class="actions text-right">
					<a href="<?=routeUrl('admin_edit_type', ['id' => $type['id']])?>" class="btn btn-xs btn-warning" data-remote-modal="#edit-type-modal">
						<span class="fa fa-edit"></span> <?=t('edit')?>
					</a>
					<a href="<?=routeUrl('admin_delete_type', ['id' => $type['id']])?>" class="btn btn-xs btn-danger" data-method="delete" data-confirm="<?=t('confirm.delete')?>">
						<span class="fa fa-times"></span> <?=t('delete')?>
					</a>
				</td>
			</tr>
		<?php endforeach; ?>
		</tbody>
	</table>
</div>
<?php $this->endSection(); ?>
