<div class="admin types container">
	<h1 class="page-header"><?=$t('types')?></h1>

	<div class="page-actions">
		<?=HTML::link(TWBS::fa('plus', $t('new_type')), $route('new_admin_type'), [
			'class' => 'btn btn-success',
			'data-overlay' => true
		])?>
	</div>

	<table class="table table-striped table-hover">
		<thead>
			<tr>
				<th><?=$t('name')?></th>
				<th></th>
			</tr>
		</thead>
		<tbody>
		<?php foreach ($types as $type): ?>
			<tr>
				<td><?=HTML::link($type->name, $route('edit_admin_type', ['id' => $type->id]))?></td>
				<td class="actions">
					<?=HTML::link(TWBS::fa('edit', $t('edit')), $route('edit_admin_type', ['id' => $type->id]), [
						'class' => 'btn btn-xs btn-warning',
						'data-overlay' => true
					])?>
					<?=HTML::link(TWBS::fa('times', $t('delete')), $route('delete_admin_type', ['id' => $type->id]), [
						'class' => 'btn btn-xs btn-danger',
						'data-confirm' => $t('confirm.delete')
					])?>
				</td>
			</tr>
		<?php endforeach; ?>
		</tbody>
	</table>
</div>
