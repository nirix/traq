<div class="admin severities container">
	<h1 class="page-header"><?=$t('severities')?></h1>

	<div class="page-actions">
		<?=HTML::link(TWBS::fa('plus', $t('new_severity')), $route('new_admin_severity'), [
			'class' => 'btn btn-success',
			'data-overlay' => true
		])?>
	</div>

	<table class="table table-striped table-hover">
		<thead>
			<tr>
				<th><?=$t('name')?></th>
				<th></th>
			</tr>
		</thead>
		<tbody>
		<?php foreach ($severities as $severity): ?>
			<tr>
				<td><?=HTML::link($severity->name, $route('edit_admin_severity', ['id' => $severity->id]))?></td>
				<td class="actions">
					<?=HTML::link(TWBS::fa('edit', $t('edit')), $route('edit_admin_severity', ['id' => $severity->id]), [
						'class' => 'btn btn-xs btn-warning',
						'data-overlay' => true
					])?>
					<?=HTML::link(TWBS::fa('times', $t('delete')), $route('edit_admin_severity', ['id' => $severity->id]), [
						'class' => 'btn btn-xs btn-danger',
						'data-confirm' => $t('confirm.delete')
					])?>
				</td>
			</tr>
		<?php endforeach; ?>
		</tbody>
	</table>
</div>
