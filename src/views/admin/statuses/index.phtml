<div class="admin statuses container">
	<h1 class="page-header"><?=t('statuses')?></h1>

	<div class="page-actions">
		<a href="<?=routeUrl('admin_new_status')?>" class="btn btn-success" data-overlay="1">
			<span class="fa fa-plus"></span> <?=t('new_status')?>
		</a>
	</div>

	<table class="table table-striped table-hover">
		<thead>
			<tr>
				<th><?=t('name')?></th>
				<th><?=t('type')?></th>
				<th></th>
			</tr>
		</thead>
		<tbody>
		<?php foreach ($statuses as $status) : ?>
			<tr>
				<td>
					<a href="<?=routeUrl('admin_edit_status', ['id' => $status['id']])?>" data-overlay="1">
						<?=$status['name']?>
					</a>
				</td>
				<td><?=t("status.type.{$status->status}")?></td>
				<td class="actions">
					<a href="<?=routeUrl('admin_edit_status', ['id' => $status['id']])?>" data-overlay="1" class="btn btn-xs btn-warning">
						<span class="fa fa-edit"></span> <?=t('edit')?>
					</a>
					<a href="<?=routeUrl('admin_delete_status', ['id' => $status['id']])?>" data-confirm="<?=t('confirm.delete')?>" class="btn btn-xs btn-danger">
						<span class="fa fa-times"></span> <?=t('delete')?>
					</a>
				</td>
			</tr>
		<?php endforeach ?>
		</tbody>
	</table>
</div>
