<?php
if (Subscription::isSubscribed($currentUser, $object)):
    echo HTML::link($t('unsubscribe'), $object->href('unsubscribe'));
else:
    echo HTML::link($t('subscribe'), $object->href('subscribe'));
endif;
